debug: False
force: False

output: ./outputs/chains/reichardt_act

sampler:
  mcmc:


theory: !defaults [ theory_cosmopower ]
likelihood: !defaults [likelihood_mflike_highL]
params: !defaults [ params_cosmology ]
prior: !defaults [ priors_cosmology ]
