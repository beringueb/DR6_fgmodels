debug: False
force: False
resume: True 

output: ./outputs/chains/reichardt_cirrus_prior

sampler:
  mcmc:


theory: !defaults [ theory_cosmopower ]
likelihood: !defaults [likelihood_mflike_highL]
# likelihood:
#   one:
#     noise: 1e-5
# params: !defaults [ params_cosmology_fixed, params_systematics_highL, params_foregrounds_common, params_foregrounds_highL ]
params: !defaults [ params_cosmology_fixed ]
prior: !defaults [ priors_cosmology, priors_systematics, priors_foregrounds ]
