debug: False
force: False

output: ../outputs/chains/MBAC_reichardt_plik_fixedcosmo

sampler:
  minimize:


theory: !defaults [ theory_cosmopower ]
likelihood: !defaults [likelihood_mflike_highL, likelihood_plikmflike ]
params: !defaults [ params_cosmology_fixed ]
prior: !defaults [ priors_cosmology, priors_systematics, priors_foregrounds ]
